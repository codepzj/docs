import { getThemeConfig } from "@sugarat/theme/node";

const blogTheme = getThemeConfig({
  author: "浩瀚星河",
  mermaid: true,
  search: false,
  themeColor: "vp-green",
  recommend: false,
});

export { blogTheme };
